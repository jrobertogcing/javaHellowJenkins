pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                // 
                echo "Build"
                mvn clean -f HelloWorld
            }
        }
        stage('Test') { 
            steps {
                // 
                echo "Test"

            }
        }
        stage('Deploy') { 
            steps {
                // 
                echo "Deploy"
                mvm package -f HelloWorld
            }
        }
    }
}
